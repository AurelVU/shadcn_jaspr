/// Theme utilities for shadcn_jaspr.
///
/// The CSS variables for theming are defined in `web/styles/globals.tw.css`.
/// Copy that file into your project's web directory and import it in your
/// Tailwind CSS configuration.
///
/// To switch between light/dark mode, toggle the `dark` class on the
/// `<html>` element.
library;
